import React from 'react'
import { Container, Table } from "react-bootstrap";

export default function MainUI() {
    return (
      <Container style={{ minHeight: "75vh", display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
        안녕하세요!!
      </Container>
    );
  }
